# 1. קח תמונה קיימת של פייתון (מערכת הפעלה קטנה עם פייתון מותקן)
FROM python:3.9-slim

# 2. הגדר את תיקיית העבודה בתוך הקונטיינר
WORKDIR /app

# 3. תעתיק את הקובץ שלנו מהמחשב לתוך הקונטיינר
COPY app.py .

# 4. הפקודה שתרוץ כשהקונטיינר עולה
CMD ["python", "app.py"]